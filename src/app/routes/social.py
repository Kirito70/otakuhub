"""Social routes stub – provides a router placeholder for imports.

The test suite only checks that the router can be imported; endpoint implementations are not required.
"""

from fastapi import APIRouter

router = APIRouter()
